[package]
name = "blink_with_C"
version = "0.1.0"
edition = "2024"

[dependencies]
cortex-m-rt = "0.7"
panic-halt = "1.0.0"

[build-dependencies]
cc = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
